{% block body %}
<div class="media text-muted pt-3">

    {% set initials = post.user.fullName|split(' ') %}

    <a href="{{ path('micro_post_blog', {'id':post.id}) }}"><img data-src="holder.js/32x32?text={{ attribute(initials,0)|first }}{{ attribute(initials,1)|first }}&bg=e83e8c&fg=fff&size=8" alt="" class="mr-2 rounded"></a>
    <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
        <span class="d-block"><strong class="text-gray-dark">@{{ post.user.username }}</strong> <small>· {{ post.time|date('d/m/Y') }}</small></span>
        {{ post.text }}
    </p>
    <small class="text-right">
        <a href="{{ path("micro_post_edit", {id: post.id}) }}">Edit</a>
        <a href="{{ path("micro_post_delete", {id: post.id}) }}">Delete</a>
    </small>

</div>
{% endblock %}